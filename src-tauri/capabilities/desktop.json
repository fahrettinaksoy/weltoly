{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "desktop",
  "description": "Yalnız masaüstü: otomatik güncelleme (updater) ve yeniden başlatma (process).",
  "platforms": ["linux", "macOS", "windows"],
  "windows": ["main"],
  "permissions": [
    "updater:default",
    "process:default"
  ]
}
